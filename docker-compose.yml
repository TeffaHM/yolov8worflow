version: '3'
services:
 metaflow-compose:
    image: metaflow
    user: 1001
    working_dir: /metaflow
    stdin_open: true
    volumes:
      - /etc/group:/etc/group:ro
    tty: true
    command: tail -f /dev/null